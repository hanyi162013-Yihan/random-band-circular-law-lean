import BernoulliSection9.Section9Results

/-!
# Bernoulli Section 9

Formalization of the terminal and arbitrary-frame small-ball arguments in
Section 9 of arXiv:2609.01295v1, using the bundled `BernoulliLinearAlgebra`
modules. The internal RRQR result proves a polynomial-loss variant with
exponent 16 rather than the exponent 4 fixed in the paper's Lemma 9.1.
-/
